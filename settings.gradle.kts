plugins {
    // Automatically downloads the Java toolchain (JDK 21) if it is not installed locally.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "PurrTechCrops"
